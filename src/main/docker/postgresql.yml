version: '2'
services:
  gpmgateway-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/GpmGateway/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=GpmGateway
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
